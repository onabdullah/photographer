/**
 * Admin toasts use the global toast (see @/Components/GlobalToast).
 * useAdminToast() is an alias for useToast() so existing admin code keeps working.
 */
export { useToast as useAdminToast } from '@/Components/GlobalToast';

